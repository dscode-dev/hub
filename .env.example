# ---------------------------------------------------------------------------
# Plataforma Hub - variaveis de ambiente (raiz do monorepo)
# Copie para .env e ajuste. Cada app tambem possui seu proprio .env.example.
# ---------------------------------------------------------------------------

# Postgres (docker compose)
POSTGRES_USER=hub
POSTGRES_PASSWORD=hub
POSTGRES_DB=plataforma_hub
POSTGRES_PORT=5442
